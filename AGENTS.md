# AGENTS.md

## 최우선 규칙: 공개 저장소

이 저장소(`JeongJaeSoon/agent-skills`)는 public이다. 어떤 파일, 커밋 메시지, PR 본문에도 회사 내부 정보를 넣지 않는다.

- 넣지 않는 것: 회사·조직 이름, 사내 저장소 이름, Slack 채널 이름과 ID, 사람 이름과 ID, PR·이슈·티켓의 URL과 키, 사내 도메인, 로그와 트랜스크립트 원문.
- 실제 운영 기록을 근거로 쓸 때는 일반화한 표현만 쓴다. 예: "Slack 리뷰 의뢰", "Jira 티켓", "사내 저장소".
- 사내 전용 설정(채널 ID, 리뷰어 별칭 같은 것)은 저장소 밖 로컬 설정 파일(`~/.config/…` 또는 `.gitignore` 대상)에서 읽게 만든다.
- 커밋 전에 스테이지된 diff를 회사·사내 서비스 이름 목록으로 grep해서 0건인지 확인한다. 목록 자체도 저장소에 두지 않고 로컬 설정에 둔다.

## 작업 방식

- 코드와 주석은 영어, 스킬 문서와 설계 문서는 한국어(기존 파일의 언어를 따른다).
- 커밋은 서명한다.
- 편집은 worktree 브랜치에서 하고, 로드되는 메인 체크아웃에는 fast-forward로만 들어간다. 운영 방식은 [docs/platform.md](docs/platform.md).
- 테스트는 README의 "테스트" 절 명령으로 돌린다.
